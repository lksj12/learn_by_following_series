services:
    react:
        build:
            context: .
            dockerfile: dockerfile.dev
        ports:
            - "5173:5173"
        volumes:
            - /usr/src/app/node_modules
            - ./:/usr/src/app
        stdin_open: true